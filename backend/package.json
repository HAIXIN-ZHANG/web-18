{
  "name": "backend",
  "version": "1.0.0",
  "description": "This is a backend server for the Top Social App",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json:server": "json-server --port 8000 --watch db.json",
    "server": "nodemon index.js",
    "dev": "concurrently \"npm run server\" \"npm run json:server\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "json-server": "^0.17.4",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0"
  }
}
